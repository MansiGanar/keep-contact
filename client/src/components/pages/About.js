import { Typography } from "@mui/material";
import React from "react";

const About = () => {
  return (
    <div>
      <Typography fontSize={18}>About this app</Typography>
      <Typography>
        This is a full stack React app for keeping contacts
      </Typography>
    </div>
  );
};

export default About;
